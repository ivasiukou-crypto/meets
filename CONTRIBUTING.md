# 🚀 Как работать с лендингом в Cursor

## Первоначальная настройка (один раз)

**Шаг 1:** Открой терминал в Cursor: **View → Terminal** (или `Ctrl+``)

**Шаг 2:** Скопируй проект к себе — введи команду:
```bash
git clone https://github.com/tucano1001/naura_meet_lp.git
```

**Шаг 3:** Открой папку в Cursor: **File → Open Folder** → выбери `naura_meet_lp`

**Шаг 4:** Установи зависимости:
```bash
npm install
```

---

## Каждый раз когда садишься работать

### ⬇️ Сначала ЗАБЕРИ последние изменения:
```bash
git pull
```
> Это скачает все изменения, которые сделал другой человек

---

## Когда закончил работу

### ⬆️ ОТПРАВЬ свои изменения:

**Шаг 1:** Добавь все изменённые файлы:
```bash
git add .
```

**Шаг 2:** Опиши что сделал:
```bash
git commit -m "Тут пишешь что изменил"
```
> Например: `git commit -m "Поменял текст в hero секции"`

**Шаг 3:** Отправь на сервер:
```bash
git push
```

---

## 📋 Короткая шпаргалка

| Когда | Команда | Что делает |
|-------|---------|------------|
| Начинаю работать | `git pull` | Скачать изменения |
| Закончил работать | `git add .` | Подготовить файлы |
| | `git commit -m "описание"` | Сохранить с описанием |
| | `git push` | Отправить на сервер |

---

## ⚠️ Если появилась ошибка при push

Значит другой человек уже отправил изменения. Просто сделай:
```bash
git pull
```
Потом снова:
```bash
git push
```

---

## 🖥️ Как запустить локально

```bash
npm run dev
```
Откроется в браузере на http://localhost:5173

Чтобы остановить — нажми `Ctrl+C` в терминале.

---

## 🌐 Продакшн

После `git push` изменения автоматически появятся на:
**https://naura-landing.vercel.app**

(деплой занимает ~1 минуту)

---

## 💡 Совет

В Cursor есть визуальная панель Git слева (иконка с веточками). Там можно:
- Видеть изменённые файлы
- Писать commit message  
- Нажимать кнопки вместо команд

Но терминал надёжнее для начала 😊

---

## 📁 Структура проекта

```
naura-landing/
├── src/
│   ├── components/
│   │   ├── Landing.tsx      ← Главный файл лендинга
│   │   └── ui/              ← UI компоненты (кнопки, карточки)
│   ├── config.ts            ← Настройки (ссылки, email)
│   └── index.css            ← Стили
├── public/                   ← Картинки, логотипы
└── package.json
```

**Основной файл для редактирования:** `src/components/Landing.tsx`
